export interface GarisHorizontalProps {
  /**
   * Warna dari garis horizontal (default #000000)
   */
  warna?: string;
  /**
   * Ukuran garis horizontal (default "kecil")
   */
  ukuran?: string;
  /**
   * Tinggi garis horizontal (default 1)
   */
  tinggi?: number;
}
